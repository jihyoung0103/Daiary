<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Firebase Auth and Firestore network calls.
         Without this, all Firebase calls fail with FirebaseNetworkException. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/btn_star"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/btn_star"
        android:supportsRtl="true"
        android:theme="@style/Theme.CapstoneLogin">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
