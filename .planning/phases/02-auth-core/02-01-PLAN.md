---
phase: 02-auth-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt
  - app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "FirebaseAuthDataSource.signIn()가 Firebase Auth에 실제 네트워크 요청을 보낸다"
    - "FirebaseAuthDataSource.signUp()가 Firebase Auth에 실제 계정 생성 요청을 보낸다"
    - "FirestoreUserDataSource.upsertUser()가 users/{uid} 문서에 merge 방식으로 쓴다"
    - "upsertUser() 실패가 예외를 상위로 전파하지 않는다 (non-fatal)"
    - "두 파일 모두 NotImplementedError를 포함하지 않는다"
  artifacts:
    - path: "app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt"
      provides: "실제 Firebase Auth 호출 (signIn/signUp)"
      contains: "signInWithEmailAndPassword"
    - path: "app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt"
      provides: "실제 Firestore upsert 호출"
      contains: "SetOptions.merge()"
  key_links:
    - from: "FirebaseAuthDataSource.signIn/signUp"
      to: "Firebase Auth 서버"
      via: "auth.signInWithEmailAndPassword().await() / auth.createUserWithEmailAndPassword().await()"
      pattern: "signInWithEmailAndPassword|createUserWithEmailAndPassword"
    - from: "FirestoreUserDataSource.upsertUser"
      to: "Firestore users/{uid}"
      via: "db.collection(\"users\").document(uid).set(data, SetOptions.merge()).await()"
      pattern: "SetOptions\\.merge\\(\\)"
---

<objective>
FirebaseAuthDataSource와 FirestoreUserDataSource의 NotImplementedError 스텁을 실제 Firebase 호출로 교체한다.

Purpose: Phase 1에서 만들어진 DataSource 스텁 2개가 실제 Firebase SDK 호출을 수행하도록 구현하여, AUTH-01(로그인)과 AUTH-02(회원가입+Firestore upsert) 요구사항의 data 레이어를 완성한다.
Output: 네트워크 호출이 가능한 FirebaseAuthDataSource.kt, FirestoreUserDataSource.kt (각각 1개 함수 교체)
</objective>

<execution_context>
@C:/Users/hdk67/spring_study/capstone_login/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hdk67/spring_study/capstone_login/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-firebase-setup/01-02-SUMMARY.md

<interfaces>
<!-- 현재 파일 상태 — 교체 대상 스텁 -->

FirebaseAuthDataSource.kt (현재 상태):
```kotlin
package com.example.capstone_login.data.source

import com.google.firebase.auth.FirebaseAuth
import com.google.firebase.auth.FirebaseAuthException
import com.google.firebase.auth.FirebaseUser
import kotlinx.coroutines.tasks.await

class FirebaseAuthDataSource {
    private val auth by lazy { FirebaseAuth.getInstance() }

    suspend fun signIn(email: String, password: String): FirebaseUser {
        throw NotImplementedError("Phase 2: implement signIn")  // ← 교체 대상
    }

    suspend fun signUp(email: String, password: String): FirebaseUser {
        throw NotImplementedError("Phase 2: implement signUp")  // ← 교체 대상
    }

    fun signOut() { auth.signOut() }
    fun currentUser(): FirebaseUser? = auth.currentUser
}
```

FirestoreUserDataSource.kt (현재 상태):
```kotlin
package com.example.capstone_login.data.source

import com.google.firebase.firestore.FirebaseFirestore
import com.google.firebase.firestore.SetOptions
import kotlinx.coroutines.tasks.await

class FirestoreUserDataSource {
    private val db by lazy { FirebaseFirestore.getInstance() }

    suspend fun upsertUser(uid: String, email: String) {
        throw NotImplementedError("Phase 2: implement upsertUser")  // ← 교체 대상
    }
}
```

AuthRepository.kt (참조 — 변경 없음):
```kotlin
// Repository는 이미 완전 구현됨. DataSource만 교체하면 전체 흐름이 동작한다.
suspend fun signIn(email: String, password: String): Result<FirebaseUser> {
    return try {
        val user = authSource.signIn(email, password)
        runCatching { userSource.upsertUser(user.uid, user.email ?: "") }
        Result.Success(user)
    } catch (e: FirebaseAuthException) { Result.Error(e) }
      catch (e: Exception) { Result.Error(e) }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FirebaseAuthDataSource — signIn/signUp NotImplementedError 교체</name>
  <files>app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt (전체 파일 — 현재 import 목록과 스텁 본문 확인)
  </read_first>
  <action>
파일 전체를 아래 내용으로 교체한다. import 목록은 현재 파일과 동일하다 (새 import 추가 없음 — `kotlinx.coroutines.tasks.await`가 이미 선언됨).

교체 후 최종 파일 전체 내용:

```kotlin
package com.example.capstone_login.data.source

import com.google.firebase.auth.FirebaseAuth
import com.google.firebase.auth.FirebaseAuthException
import com.google.firebase.auth.FirebaseUser
import kotlinx.coroutines.tasks.await

/**
 * Data source wrapping Firebase Authentication SDK.
 * ONLY this class imports com.google.firebase.auth.*.
 * Uses lazy initialization to avoid FirebaseApp pre-initialization crash.
 */
class FirebaseAuthDataSource {

    private val auth by lazy { FirebaseAuth.getInstance() }

    /**
     * Sign in with email and password.
     * @throws FirebaseAuthException on auth failure (wrong password, user not found, etc.)
     * @throws IllegalStateException if Firebase returns a null user after success
     */
    suspend fun signIn(email: String, password: String): FirebaseUser {
        return auth.signInWithEmailAndPassword(email, password).await().user
            ?: throw IllegalStateException("Auth succeeded but user is null")
    }

    /**
     * Create account with email and password.
     * @throws FirebaseAuthException on auth failure (email in use, weak password, etc.)
     * @throws IllegalStateException if Firebase returns a null user after success
     */
    suspend fun signUp(email: String, password: String): FirebaseUser {
        return auth.createUserWithEmailAndPassword(email, password).await().user
            ?: throw IllegalStateException("Auth succeeded but user is null")
    }

    fun signOut() {
        auth.signOut()
    }

    fun currentUser(): FirebaseUser? = auth.currentUser
}
```

변경 요약:
- signIn(): `throw NotImplementedError(...)` → `auth.signInWithEmailAndPassword(email, password).await().user ?: throw IllegalStateException(...)`
- signUp(): `throw NotImplementedError(...)` → `auth.createUserWithEmailAndPassword(email, password).await().user ?: throw IllegalStateException(...)`
- 주석 내 TODO Phase 2 줄 제거 (스텁 주석 정리)
  </action>
  <acceptance_criteria>
1. 파일에 "NotImplementedError" 문자열이 없다:
   `grep -c "NotImplementedError" app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt`
   → 0 반환

2. signIn 구현이 존재한다:
   `grep -c "signInWithEmailAndPassword" app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt`
   → 1 반환

3. signUp 구현이 존재한다:
   `grep -c "createUserWithEmailAndPassword" app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt`
   → 1 반환

4. .await() import가 유지된다:
   `grep -c "kotlinx.coroutines.tasks.await" app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt`
   → 1 반환
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>FirebaseAuthDataSource.kt에 NotImplementedError가 없고, signIn/signUp이 .await() 패턴으로 구현되었으며, compileDebugKotlin이 성공한다.</done>
</task>

<task type="auto">
  <name>Task 2: FirestoreUserDataSource — upsertUser NotImplementedError 교체 (non-fatal 래핑 포함)</name>
  <files>app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt (전체 파일 — 현재 import 목록 확인)
  </read_first>
  <action>
파일 전체를 아래 내용으로 교체한다. `FieldValue` import를 추가해야 한다 (현재 파일에 없음).

교체 후 최종 파일 전체 내용:

```kotlin
package com.example.capstone_login.data.source

import android.util.Log
import com.google.firebase.firestore.FieldValue
import com.google.firebase.firestore.FirebaseFirestore
import com.google.firebase.firestore.SetOptions
import kotlinx.coroutines.tasks.await

/**
 * Data source wrapping Firebase Firestore SDK.
 * ONLY this class imports com.google.firebase.firestore.*.
 * Uses lazy initialization to avoid FirebaseApp pre-initialization crash.
 */
class FirestoreUserDataSource {

    private val db by lazy { FirebaseFirestore.getInstance() }

    /**
     * Upsert user document at users/{uid}.
     * Uses set() with SetOptions.merge() — idempotent, safe to call on every login.
     * Non-fatal: exceptions are caught and logged; callers receive success even if Firestore write fails.
     * This ensures auth success is not blocked by Firestore offline/permission errors.
     */
    suspend fun upsertUser(uid: String, email: String) {
        try {
            val data = mapOf(
                "email" to email,
                "lastLoginAt" to FieldValue.serverTimestamp()
            )
            db.collection("users").document(uid).set(data, SetOptions.merge()).await()
        } catch (e: Exception) {
            Log.w("FirestoreUserDataSource", "upsertUser failed (non-fatal): ${e.message}")
            // Do not rethrow — Firestore write failure must not block auth success
        }
    }
}
```

변경 요약:
- `import com.google.firebase.firestore.FieldValue` 추가
- `import android.util.Log` 추가
- `throw NotImplementedError(...)` → try/catch 블록으로 교체
  - try: `FieldValue.serverTimestamp()` 포함 data map 생성 → `set(data, SetOptions.merge()).await()`
  - catch(Exception): `Log.w(...)` 출력 후 예외를 rethrow하지 않음 (non-fatal 보장)

주의: Repository의 `runCatching { userSource.upsertUser(...) }` 래핑과 이중 보호가 되지만,
DataSource 자체에서도 예외를 삼키도록 구현하는 것이 "non-fatal upsert" 설계 원칙에 맞다.
  </action>
  <acceptance_criteria>
1. NotImplementedError 없음:
   `grep -c "NotImplementedError" app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt`
   → 0 반환

2. SetOptions.merge() 사용:
   `grep -c "SetOptions.merge" app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt`
   → 1 반환

3. FieldValue.serverTimestamp() 사용:
   `grep -c "serverTimestamp" app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt`
   → 1 반환

4. 예외를 rethrow하지 않음 (catch 블록이 있고 throw 없음):
   `grep -c "catch" app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt`
   → 1 반환 (catch 블록 존재)
   파일 내 catch 블록 안에 `throw e` 또는 `throw ex` 없음 — 수동 확인
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>FirestoreUserDataSource.kt에 NotImplementedError가 없고, upsertUser가 SetOptions.merge()와 FieldValue.serverTimestamp()를 사용하며, 예외를 catch하고 Log.w만 출력한 뒤 rethrow하지 않는다. compileDebugKotlin 성공.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataSource → Firebase Auth 서버 | 이메일/비밀번호 자격증명이 네트워크를 통해 Firebase로 전송됨 |
| DataSource → Firestore 서버 | uid, email, serverTimestamp가 네트워크를 통해 Firestore로 전송됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | FirebaseAuthDataSource.signIn/signUp | mitigate | 함수 파라미터(email, password)를 절대 Log.d/Log.w/Log.e로 출력하지 않음. catch 블록에서도 e.message만 출력 |
| T-02-01-02 | Denial of Service | FirebaseAuthDataSource.signIn | accept | Firebase 자체 rate limiting (ERROR_TOO_MANY_REQUESTS) 적용됨 — 추가 구현 불필요 |
| T-02-01-03 | Tampering | FirestoreUserDataSource.upsertUser | mitigate | SetOptions.merge() 사용으로 기존 필드 덮어쓰기 방지 (email, lastLoginAt만 갱신). Firestore Security Rules는 Phase 4에서 강화 |
| T-02-01-04 | Information Disclosure | FirestoreUserDataSource — Log.w | mitigate | Log.w 출력에 uid, email 포함 금지. e.message만 출력 (이미 action에 반영됨) |
</threat_model>

<verification>
## Phase 2 Plan 01 검증 체크리스트

```bash
# 1. 컴파일 성공
./gradlew :app:compileDebugKotlin

# 2. NotImplementedError 잔재 없음 (두 파일 모두)
grep -rn "NotImplementedError" app/src/main/kotlin/com/example/capstone_login/data/source/

# 3. Firebase 호출 패턴 존재
grep -n "signInWithEmailAndPassword\|createUserWithEmailAndPassword" \
  app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt

grep -n "SetOptions.merge\|serverTimestamp" \
  app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt

# 4. .await() import 유지
grep -n "kotlinx.coroutines.tasks.await" \
  app/src/main/kotlin/com/example/capstone_login/data/source/FirebaseAuthDataSource.kt \
  app/src/main/kotlin/com/example/capstone_login/data/source/FirestoreUserDataSource.kt
```
</verification>

<success_criteria>
- FirebaseAuthDataSource.kt: signIn()과 signUp() 모두 `.await()` 패턴으로 구현됨 (NotImplementedError 없음)
- FirestoreUserDataSource.kt: upsertUser()가 SetOptions.merge() + FieldValue.serverTimestamp() 사용, 예외 non-fatal 처리
- `./gradlew :app:compileDebugKotlin` 성공
- 두 파일 모두 비밀번호/uid/이메일을 로그에 출력하지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/02-auth-core/02-01-SUMMARY.md` 생성.
@C:/Users/hdk67/spring_study/capstone_login/.claude/get-shit-done/templates/summary.md 참고.
</output>
