---
phase: 04-polish-release-validation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - firestore.rules
  - app/build.gradle.kts
  - app/proguard-rules.pro
requirements: []
---

<objective>
Firestore 보안 규칙 파일 작성, R8 minification 활성화, Firebase ProGuard 규칙 추가.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: firestore.rules — 인증된 사용자만 본인 문서 접근</name>
  <files>firestore.rules</files>
  <action>
users/{userId} 문서: request.auth != null && request.auth.uid == userId 조건으로 read/write 허용.
테스트 모드(allow all) 규칙에서 프로덕션 규칙으로 전환.
  </action>
</task>

<task type="auto">
  <name>Task 2: app/build.gradle.kts — release isMinifyEnabled = true</name>
  <files>app/build.gradle.kts</files>
  <action>
release 빌드타입: isMinifyEnabled = false → true
  </action>
</task>

<task type="auto">
  <name>Task 3: proguard-rules.pro — Firebase + 코루틴 규칙</name>
  <files>app/proguard-rules.pro</files>
  <action>
- Kotlin coroutines volatile fields keep 규칙 추가
- @Keep annotation 지원 keepattributes 추가
  </action>
</task>

</tasks>

<success_criteria>
- firestore.rules 파일 존재, request.auth.uid == userId 조건 포함
- app/build.gradle.kts release.isMinifyEnabled = true
- proguard-rules.pro에 kotlinx.coroutines volatile fields keep 존재
</success_criteria>
