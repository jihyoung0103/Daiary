---
phase: 03-navigation-auto-login
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - app/src/main/kotlin/com/example/capstone_login/ui/auth/LoginFragment.kt
  - app/src/main/kotlin/com/example/capstone_login/ui/calendar/CalendarFragment.kt
requirements:
  - UI-03
---

<objective>
로그인/회원가입 성공 후 back 버튼으로 LoginFragment에 돌아갈 수 없게 하고,
로그아웃 후 CalendarFragment가 back stack에 남지 않도록 popUpTo를 적용한다.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: LoginFragment Success navigate에 popUpTo 추가</name>
  <files>
    app/src/main/kotlin/com/example/capstone_login/ui/auth/LoginFragment.kt
  </files>
  <action>
Success 브랜치의 findNavController().navigate() 호출에 NavOptions 추가:
  NavOptions.Builder().setPopUpTo(R.id.loginFragment, true).build()
  </action>
</task>

<task type="auto">
  <name>Task 2: CalendarFragment 로그아웃 navigate에 popUpTo 추가</name>
  <files>
    app/src/main/kotlin/com/example/capstone_login/ui/calendar/CalendarFragment.kt
  </files>
  <action>
logoutButton 클릭의 navigate() 호출에 NavOptions 추가:
  NavOptions.Builder().setPopUpTo(R.id.calendarFragment, true).build()
  </action>
</task>

</tasks>

<success_criteria>
- LoginFragment navigate에 setPopUpTo(loginFragment) 존재
- CalendarFragment navigate에 setPopUpTo(calendarFragment) 존재
- 두 파일에 androidx.navigation.NavOptions import 존재
</success_criteria>
