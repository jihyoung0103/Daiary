---
phase: 03-navigation-auto-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/capstone_login/ui/MainActivity.kt
requirements:
  - AUTH-04
---

<objective>
MainActivity.onCreate()에서 FirebaseAuth.currentUser를 확인하여 이미 로그인된 사용자를 CalendarFragment로 즉시 라우팅한다.
앱 재시작 시 LoginFragment가 잠깐 보이고 넘어가는 현상 없이 CalendarFragment로 바로 진입.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: MainActivity 자동 로그인 라우팅 구현</name>
  <files>
    app/src/main/kotlin/com/example/capstone_login/ui/MainActivity.kt
  </files>
  <action>
- savedInstanceState == null 체크: Activity 재생성(화면 회전 등) 시 중복 navigate 방지
- NavHostFragment를 통해 NavController 획득 (findFragmentById 방식)
- currentUser != null → navigate(calendarFragment, NavOptions popUpTo loginFragment inclusive=true)
- loginFragment를 back stack에서 제거하므로 calendarFragment에서 back → 앱 종료
  </action>
</task>

</tasks>

<success_criteria>
- savedInstanceState == null 가드 존재
- FirebaseAuth.getInstance().currentUser 체크 존재
- NavHostFragment 방식으로 NavController 획득
- setPopUpTo(R.id.loginFragment, true) 적용
</success_criteria>
